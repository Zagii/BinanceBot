{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "node-terminal",
            "name": "Run Script: app",
            "request": "launch",
            "command": "node app",
            "cwd": "${workspaceFolder}"
        }
    ]
}